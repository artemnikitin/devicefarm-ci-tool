dependencies:
  override:
    - echo "Start collecting dependencies ..."
    - go get -u -v github.com/artemnikitin/devicefarm-ci-tool
    - go build -v

test:
  override:
    - echo "Start tests ..."
    - go test $(go list ./... | grep -v /vendor/)